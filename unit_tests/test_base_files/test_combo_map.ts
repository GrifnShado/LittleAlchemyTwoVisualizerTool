// Combo map print fxn test

// The number of tests (?)
//const tests: number[] = [];

function testPrintComboMaps(){

    // Create a combo map

    // Create some fake element data

    // Iterate through the idToObj, nameToId, and rowToIdArr maps

    // Send a report if it fails

}//testPrintComboMaps

function testPrintConstraintMaps(){

    // Create a combo map

    // Create fake element data

    // Iterate through the constraint maps

    // Send a report if it fails

}//testConstraintMaps

